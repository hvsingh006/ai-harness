# Prototype currently uses no API keys.
# Future provider/API integrations belong here and must never be committed.
HARNESS_PORT=4317

# Optional: override the permanent workspace root. Keep this outside the application checkout.
# HARNESS_WORKSPACE_ROOT=C:\\Users\\you\\Documents\\AI Harness
