{
  "name": "ai-harness",
  "version": "0.6.3",
  "private": true,
  "type": "module",
  "description": "Local Project Spaces that retain files and ChatGPT/Gemini history across chats and services.",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test",
    "import": "node scripts/import.mjs",
    "doctor": "node scripts/doctor.mjs",
    "backup": "node scripts/backup.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
